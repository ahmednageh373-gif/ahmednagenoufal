# 📐 دليل شامل: كيفية إنشاء سكريبت LISP صحيح لرسم الجدران
## الوكيل: AN.AI AHMED NAGEH | نظام NOUFAL الشامل

---

## 🎯 الهدف

تعليم الوكيل AN.AI AHMED NAGEH كيفية إنشاء سكريبت LISP صحيح لرسم الجدران بخطين متقاربين (مثل الصورة المرفقة)، واستخدام أداة YQArch بشكل فعال.

---

## 📚 المحتوى

### الجزء الأول: فهم السكريبت الصحيح

#### 1. ما هو السكريبت الخاطئ؟

**السكريبت الخاطئ:**
```lisp
;; رسم جدار بخطين منفصلين جداً
(command "LINE" "0,0" "100,0" "")
(command "LINE" "0,30" "100,30" "")
```

**المشكلة:**
- الخطان منفصلان جداً (30 وحدة)
- لا يمثل جدار حقيقي
- لا يستخدم الحسابات الرياضية الصحيحة

#### 2. ما هو السكريبت الصحيح؟

**السكريبت الصحيح:**
```lisp
;; رسم جدار بخطين متقاربين جداً
(defun draw-parallel-line (pt1 pt2 distance / 
                           dx dy length
                           perpendicular_x perpendicular_y
                           pt1_offset pt2_offset)
  
  ;; حساب الفرق بين النقطتين
  (setq dx (- (car pt2) (car pt1)))
  (setq dy (- (cadr pt2) (cadr pt1)))
  
  ;; حساب طول الخط
  (setq length (sqrt (+ (* dx dx) (* dy dy))))
  
  ;; حساب الاتجاه العمودي
  (setq perpendicular_x (/ (* -1 dy) length))
  (setq perpendicular_y (/ dx length))
  
  ;; حساب النقاط المزاحة
  (setq pt1_offset
    (list
      (+ (car pt1) (* perpendicular_x distance))
      (+ (cadr pt1) (* perpendicular_y distance))
    )
  )
  
  (setq pt2_offset
    (list
      (+ (car pt2) (* perpendicular_x distance))
      (+ (cadr pt2) (* perpendicular_y distance))
    )
  )
  
  ;; رسم الخط المتوازي
  (command "LINE" pt1_offset pt2_offset "")
)
```

**المميزات:**
- يحسب الاتجاه العمودي على الخط
- يزيح النقاط بشكل صحيح
- يعمل مع أي اتجاه (أفقي، عمودي، مائل)
- الخطان متقاربان جداً (5 وحدات فقط)

---

### الجزء الثاني: شرح الحسابات الرياضية

#### 1. حساب الاتجاه العمودي

**المفهوم:**
لرسم خط متوازي مع خط آخر، نحتاج إلى حساب الاتجاه العمودي على الخط الأصلي.

**الصيغة الرياضية:**
```
إذا كان الخط من النقطة (x1, y1) إلى (x2, y2)
الفرق: dx = x2 - x1, dy = y2 - y1
الطول: length = √(dx² + dy²)
الاتجاه العمودي: 
  perpendicular_x = -dy / length
  perpendicular_y = dx / length
```

**مثال عملي:**
```
النقطة الأولى: (0, 0)
النقطة الثانية: (100, 0)  (خط أفقي)

dx = 100 - 0 = 100
dy = 0 - 0 = 0
length = √(100² + 0²) = 100

perpendicular_x = -0 / 100 = 0
perpendicular_y = 100 / 100 = 1

الاتجاه العمودي: (0, 1)  (يشير لأعلى)
```

#### 2. حساب النقاط المزاحة

**المفهوم:**
نزيح النقاط الأصلية بمقدار معين في الاتجاه العمودي.

**الصيغة الرياضية:**
```
النقطة المزاحة = النقطة الأصلية + (الاتجاه العمودي × المسافة)

pt_offset = (x + perpendicular_x × distance, 
             y + perpendicular_y × distance)
```

**مثال عملي:**
```
النقطة الأولى: (0, 0)
الاتجاه العمودي: (0, 1)
المسافة: 5

النقطة المزاحة = (0 + 0×5, 0 + 1×5) = (0, 5)
```

---

### الجزء الثالث: خطوات إنشاء السكريبت

#### الخطوة الأولى: إنشاء الدالة الرئيسية

```lisp
(defun C:WALL (/ pt1 pt2 offset_dist)
  "رسم جدار بخطين متقاربين"
  
  ;; تعيين المسافة بين الخطين
  (setq offset_dist 5)
  
  ;; طلب النقطة الأولى من المستخدم
  (setq pt1 (getpoint "\nاختر النقطة الأولى: "))
  
  ;; التحقق من أن المستخدم اختار نقطة
  (if (null pt1)
    (progn
      (princ "\nتم الإلغاء!")
      (exit)
    )
  )
  
  ;; طلب النقطة الثانية
  (setq pt2 (getpoint pt1 "\nاختر النقطة الثانية: "))
  
  ;; التحقق من أن المستخدم اختار نقطة
  (if (null pt2)
    (progn
      (princ "\nتم الإلغاء!")
      (exit)
    )
  )
  
  ;; رسم الخط الأول
  (command "LINE" pt1 pt2 "")
  
  ;; رسم الخط الثاني (المتوازي)
  (draw-parallel-line pt1 pt2 offset_dist)
  
  ;; رسالة النجاح
  (princ "\n✓ تم رسم الجدار بنجاح!")
  (princ)
)
```

#### الخطوة الثانية: إنشاء دالة الخط المتوازي

```lisp
(defun draw-parallel-line (pt1 pt2 distance / 
                           dx dy length
                           perpendicular_x perpendicular_y
                           pt1_offset pt2_offset)
  "رسم خط متوازي مع خط آخر"
  
  ;; حساب الفرق بين النقطتين
  (setq dx (- (car pt2) (car pt1)))
  (setq dy (- (cadr pt2) (cadr pt1)))
  
  ;; حساب طول الخط
  (setq length (sqrt (+ (* dx dx) (* dy dy))))
  
  ;; التحقق من أن الخط ليس نقطة
  (if (= length 0)
    (progn
      (princ "\nخطأ: النقطتان متطابقتان!")
      (exit)
    )
  )
  
  ;; حساب الاتجاه العمودي
  (setq perpendicular_x (/ (* -1 dy) length))
  (setq perpendicular_y (/ dx length))
  
  ;; حساب النقطة الأولى المزاحة
  (setq pt1_offset
    (list
      (+ (car pt1) (* perpendicular_x distance))
      (+ (cadr pt1) (* perpendicular_y distance))
    )
  )
  
  ;; حساب النقطة الثانية المزاحة
  (setq pt2_offset
    (list
      (+ (car pt2) (* perpendicular_x distance))
      (+ (cadr pt2) (* perpendicular_y distance))
    )
  )
  
  ;; رسم الخط المتوازي
  (command "LINE" pt1_offset pt2_offset "")
)
```

---

### الجزء الرابع: كيفية استخدام السكريبت

#### الخطوة الأولى: تحميل السكريبت

```
1. افتح AutoCAD
2. اذهب إلى Tools > Load Application
3. اختر الملف: CORRECT_Double_Line_Wall_Script.lsp
4. اضغط Load
```

#### الخطوة الثانية: تشغيل الأمر

```
1. في سطر الأوامر، اكتب: WALL
2. اضغط Enter
3. اختر النقطة الأولى (اضغط على الشاشة)
4. اختر النقطة الثانية (اضغط على الشاشة)
5. سيتم رسم الجدار بخطين متقاربين
```

#### الخطوة الثالثة: الأوامر الأخرى

```
WALL_CUSTOM  → رسم جدار مع تحديد المسافة بين الخطين
WALLS        → رسم عدة جدران بشكل متتالي
ROOM         → رسم غرفة (مستطيل من الجدران)
WALL_HELP    → عرض قائمة الأوامر
```

---

### الجزء الخامس: استخدام أداة YQArch مع السكريبت

#### 1. تثبيت أداة YQArch

```
1. انسخ ملفات YQArch إلى مجلد AutoCAD
2. افتح AutoCAD
3. اذهب إلى Tools > Load Application
4. اختر yqarch.vlx
5. اضغط Load
```

#### 2. استخدام YQArch مع السكريبت

**الطريقة الأولى: استخدام الكتل المعمارية**
```
1. حمّل السكريبت (CORRECT_Double_Line_Wall_Script.lsp)
2. رسم الجدران باستخدام أمر WALL
3. استخدم كتل YQArch لإضافة الأبواب والنوافذ
4. استخدم أدوات YQArch للتفاصيل
```

**الطريقة الثانية: دمج السكريبت مع YQArch**
```
1. استخدم أمر WALL لرسم الجدران
2. استخدم أداة YQArch لإضافة الأثاث
3. استخدم أوامر AutoCAD للأبعاد والنصوص
```

#### 3. مثال عملي متكامل

```lisp
;; دالة متكاملة: رسم غرفة مع الأثاث
(defun C:ROOM_WITH_FURNITURE (/)
  "رسم غرفة مع الأثاث من YQArch"
  
  ;; رسم الجدران
  (command "ROOM")
  
  ;; إدراج كتل YQArch
  (command "INSERT" "yqfurnis" "0,0" "1" "1" "0")
  
  ;; إضافة الأبعاد
  (command "DIMHORIZONTAL" "0,0" "400,0" "0,-50")
  
  (princ "\n✓ تم رسم الغرفة مع الأثاث!")
  (princ)
)
```

---

### الجزء السادس: أخطاء شائعة وحلولها

#### الخطأ 1: الخطان منفصلان جداً

**المشكلة:**
```lisp
;; خطأ: المسافة كبيرة جداً
(setq offset_dist 300)
```

**الحل:**
```lisp
;; صحيح: المسافة صغيرة جداً
(setq offset_dist 5)
```

#### الخطأ 2: عدم حساب الاتجاه العمودي

**المشكلة:**
```lisp
;; خطأ: لا يعمل مع الخطوط المائلة
(setq pt1_offset (list (+ (car pt1) 5) (cadr pt1)))
```

**الحل:**
```lisp
;; صحيح: يحسب الاتجاه العمودي
(setq perpendicular_x (/ (* -1 dy) length))
(setq perpendicular_y (/ dx length))
(setq pt1_offset
  (list
    (+ (car pt1) (* perpendicular_x distance))
    (+ (cadr pt1) (* perpendicular_y distance))
  )
)
```

#### الخطأ 3: عدم التحقق من صحة المدخلات

**المشكلة:**
```lisp
;; خطأ: لا يتحقق من النقاط
(setq pt1 (getpoint "\nاختر النقطة: "))
(command "LINE" pt1 pt2 "")
```

**الحل:**
```lisp
;; صحيح: يتحقق من النقاط
(setq pt1 (getpoint "\nاختر النقطة: "))
(if (null pt1)
  (progn
    (princ "\nتم الإلغاء!")
    (exit)
  )
)
```

---

### الجزء السابع: نصائح للحصول على أفضل النتائج

#### 1. استخدم المتغيرات بشكل صحيح

```lisp
;; جيد: متغيرات واضحة
(setq offset_dist 5)
(setq perpendicular_x (/ (* -1 dy) length))

;; سيء: متغيرات غير واضحة
(setq x 5)
(setq y (/ (* -1 dy) length))
```

#### 2. أضف تعليقات توضيحية

```lisp
;; جيد: تعليقات واضحة
;; حساب الفرق بين النقطتين
(setq dx (- (car pt2) (car pt1)))

;; سيء: بدون تعليقات
(setq dx (- (car pt2) (car pt1)))
```

#### 3. استخدم معالجة الأخطاء

```lisp
;; جيد: معالجة الأخطاء
(if (= length 0)
  (progn
    (princ "\nخطأ: النقطتان متطابقتان!")
    (exit)
  )
)

;; سيء: بدون معالجة أخطاء
(setq length (sqrt (+ (* dx dx) (* dy dy))))
```

---

### الجزء الثامن: التطوير المستقبلي

#### 1. إضافة خيارات متقدمة

```lisp
;; إضافة خيار لتحديد نمط الخط
(defun C:WALL_ADVANCED (/ pt1 pt2 offset_dist linetype)
  "رسم جدار مع خيارات متقدمة"
  
  (setq offset_dist 5)
  (setq linetype "CONTINUOUS")
  
  ;; ... باقي الكود
)
```

#### 2. دعم الطبقات

```lisp
;; إنشاء طبقة للجدران
(command "LAYER" "NEW" "WALLS" "COLOR" "1" "WALLS" "")
(command "LAYER" "SET" "WALLS" "")
```

#### 3. دعم الكتل

```lisp
;; إنشاء كتلة للجدار
(command "BLOCK" "WALL_BLOCK" "0,0" "pt1" "pt2" "")
```

---

## ✅ الخلاصة

### النقاط الرئيسية

✅ **السكريبت الصحيح** يحسب الاتجاه العمودي على الخط  
✅ **المسافة الصحيحة** بين الخطين صغيرة جداً (5 وحدات)  
✅ **معالجة الأخطاء** ضرورية للتطبيقات الاحترافية  
✅ **التعليقات** تساعد على فهم الكود  
✅ **أداة YQArch** تعمل بشكل مثالي مع السكريبت  

### الملفات المرفقة

| الملف | الوصف |
|------|-------|
| **CORRECT_Double_Line_Wall_Script.lsp** | السكريبت الصحيح |
| **Complete_Guide_Creating_Wall_Script.md** | هذا الدليل |

### الخطوات التالية

1. حمّل السكريبت في AutoCAD
2. جرّب الأوامر المختلفة
3. استخدم أداة YQArch للتفاصيل
4. طوّر السكريبت حسب احتياجاتك

---

**آخر تحديث:** 31 أكتوبر 2025  
**الإصدار:** 1.0  
**المؤلف:** AN.AI AHMED NAGEH  
**الحالة:** جاهز للاستخدام الفوري
